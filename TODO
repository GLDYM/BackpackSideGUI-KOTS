最优方案是：保留现在的悬浮框作为纯客户端视图，把“存入/取出”做成服务端权威的虚拟槽交互，而不是在客户端直接修改
  ItemStack。

  从当前代码结构看，建议采用下面的分层方式：

  1. 悬浮框只负责显示和产生操作请求

  现在的 BackpackOverlayWidget 已经负责布局、滚动和按钮。后续可以给每个悬浮框槽增加命中检测：

  - 左键：拿起/放入
  - 右键：拿一半或放入一个
  - Shift+点击：在玩家背包和悬浮框之间快速转移
  - 可选：增加“存入全部”和“取出全部”按钮

  客户端发送的应该是类似“操作类型 + 逻辑槽位 + 鼠标按钮/数量”的请求，而不是把客户端的物品内容当作最终结果。

  2. 服务端复用现有 BackpackResolver

  服务端收到请求后，重新通过 BackpackResolver.resolve(player) 获取当前背包，并检查：

  - 玩家仍然拥有该背包；
  - 背包没有被替换或移动；
  - 请求的槽位在合法范围内；
  - 背包能力实现了可修改接口；
  - 物品是否满足 IItemHandler.isItemValid；
  - 槽位数量和堆叠上限是否合法。

  不能信任客户端传来的背包内容、数量或背包槽位身份。当前同步数据只能用于显示，不能作为交易依据。

  3. 抽出一个统一的转移服务

  建议将实际逻辑集中到一个服务类中，而不是把逻辑写进悬浮框或网络处理器。核心操作包括：

  - clickBackpackSlot
  - depositFromPlayerInventory
  - withdrawToPlayerInventory
  - depositAll
  - withdrawAll

  存入时使用 IItemHandler.insertItem(..., false)，取出时使用 extractItem(..., false)，并优先合并玩家已有堆叠。取出后如果
  玩家背包放不下，应将剩余物品放回背包，避免凭空丢失；是否允许掉落到地面应作为明确的配置或特殊操作，而不是默认行为。

  4. 光标物品应以服务端容器状态为准

  悬浮框显示在其他容器界面旁边，因此不能只依赖客户端自己的 carried stack。应以服务端当前 containerMenu 的 carried stack
  为准，并在操作完成后更新它。

  如果希望实现接近原版容器的完整行为，最好复用 Minecraft 的点击语义：左键、右键、Shift、双击收集、拖拽分配等都由服务端统
  一处理。不要为每种组合编写互相独立的转移逻辑，否则很容易出现堆叠、回滚和同步不一致。

  5. 同步应改为“操作后立即同步”

  当前 SideBackpackClient 每 4 tick 发送一次 OpenBackpackPayload 请求，相当于持续轮询。实现存取后虽然能工作，但不是最
  优：

  - 增加不必要的网络流量；
  - 存取操作和同步可能乱序；
  - 多人或高延迟环境下容易看到旧数据。

  更好的方式是：

  - 打开或检测到背包时同步一次；
  - 每次服务端成功或拒绝处理操作后立即发送 BackpackSyncPayload；
  - 背包内容发生外部变化时，再通过低频兜底同步或事件触发同步；
  - 同时调用当前容器的 broadcastChanges()，保证底层容器界面和悬浮框一致。

  6. 不建议直接扩展当前 BackpackMenu

  仓库中的 BackpackMenu 目前更像独立背包界面的基础设施，但悬浮框实际显示在其他容器屏幕上。直接把悬浮框槽追加到现有容器菜
  单会带来：

  - 菜单槽位编号冲突；
  - 与 Sophisticated Backpacks 原菜单耦合；
  - 不同容器界面下布局和点击语义不一致；
  - 兼容其他模组容器的成本较高。

  因此，当前场景更适合“虚拟槽 + 自定义网络操作 + 服务端转移服务”。只有在计划让悬浮框成为一个完整、独立的背包界面时，才值
  得把它正式做成一个 AbstractContainerMenu。

  推荐的用户体验是：

  - 每个背包槽支持原版式左键/右键交互；
  - 悬浮框底部增加“存入全部”和“取出全部”两个按钮；
  - Shift+点击用于单个物品快速转移；
  - 背包满或玩家背包满时显示提示，并保留未转移物品；
  - 所有操作完成后由服务端立即同步。

  一句话概括：以现有 BackpackResolver + IItemHandler 为核心，新增服务端权威的转移事务层，悬浮框只发送操作请求并接收同步
  结果。这比在客户端直接改物品，或把悬浮框硬塞进现有菜单，兼容性、安全性和后续扩展性都更好。